# Linux 学习路线计划（终极融合版）

> 制定日期: 2026-06-02
> 修订日期: 2026-06-10
> 核心变更: 融合"机器人底层"与"AI芯片底层"双路线，打造"具身智能端侧系统工程师"核心竞争力
> 终极目标: 自动驾驶/机器人底层系统工程师 & AI芯片(NPU)驱动/部署系统工程师

---

## 一、总体策略（融合版）

```
原策略: 驱动实验逐个啃 → 纯粹做机器人 → 或者纯粹搞AI
融合策略: 以"数据流"为核心，构建从传感器到NPU再到执行器的全链路底座

核心认知:
  机器人底层 = 解决"数据如何实时获取与执行"
  AI芯片底层 = 解决"大算力数据如何零拷贝搬运与异步计算"
  融合点     = 具身智能底座（传感器零拷贝进NPU，NPU异步触发实时控制）

三条线:
  ① ★ 闭环大项目 — 具身智能端侧底座（涵盖感知-推理-执行全链路）
  ②   理论补充 — xv6 关键 Lab（只补中断/锁/内存映射）
  ③   查阅工具 — 小林coding + 内核源码（当字典翻）
```

---

## 二、项目路线图（分层架构，18 个月）

```
现在（已完成实验6~13）
  │
  ▼
┌─────────────────────────────────────────────────────────┐
│ 阶段一：感知与控制层（2个月）—— 机器人视角               │
│   让机器人的"眼睛"和"手脚"动起来，掌握内核中断机制       │
│                                                          │
│ 做：IMU(I2C)驱动 + 超声波(中断)驱动 + PWM舵机驱动        │
│     + ROS2 传感器节点集成                                │
│ 补：中断(14) + I2C(22) + 阻塞/异步通知(15)              │
│ 产出：GitHub repo(robot-perception-layer) + ROS2 实时可视化│
└─────────────────────────────────────────────────────────┘
  │ 传感器数据就绪
  ▼
┌─────────────────────────────────────────────────────────┐
│ 阶段二：数据高速公路层（4个月）—— AI芯片视角             │
│   解决数据如何"零延迟"送给NPU，打通异构内存              │
│                                                          │
│ 做：DMA驱动 + 内存池管理 + mmap零拷贝接口 + PREEMPT_RT   │
│ 补：内存映射(mmap机制) + DMA框架                         │
│ 产出：零拷贝内存管理模块，ROS2通过mmap直接读取DMA缓冲区  │
│ 论文素材：基于mmap+DMA的传感器数据零拷贝采集架构          │
└─────────────────────────────────────────────────────────┘
  │ 数据搬运通路就绪
  ▼
┌─────────────────────────────────────────────────────────┐
│ 阶段三：算力释放与闭环层（6个月）—— 软硬协同视角         │
│   将AI推理接入系统，解决"推理慢"与"控制快"的实时矛盾     │
│                                                          │
│ 做：NPU驱动框架模拟(字符设备) + ONNX自定义算子           │
│     + 中断驱动的异步推理流水线 + 实时控制闭环             │
│ 产出：硕士论文 + 校招核弹级项目                          │
│ 面试话术：从ioctl下发任务→DMA搬数据→NPU计算→中断回调控制 │
└─────────────────────────────────────────────────────────┘
```

---

## 三、驱动实验取舍策略（面向双路线增强版）

只学构建全链路必须的内核机制，其余统统跳过：

| 待遇 | 实验 | 原因（双路线视角） |
|------|------|---------------------|
| 🔥 **立刻补** | 14_中断 | NPU计算完成通知 & 超声波测距，异构计算生命线 |
| 🔥 **立刻补** | 15_阻塞IO与异步通知 | 用户态等待NPU/传感器的标准机制，必考 |
| 🔥 **立刻补** | mmap与内存映射(重点) | AI芯片岗核心：用户态零拷贝访问NPU/DMA内存的唯一途径 |
| 🔥 **立刻补** | 22_i2c | IMU/雷达传感器标配 |
| 🟡 **做项目时补** | 21_input / 28_iio | 规范化传感器框架，做IMU时补 |
| 🟡 **做项目时补** | 23_spi | 高性能传感器用，按需 |
| ⏭ **跳过** | 18/19_platform/dts | 已会，重复 |
| ⏭ **跳过** | 24_multitouch | 与目标方向完全无关 |
| ⏭ **跳过** | 25/26_ramdisk/块设备 | 不做存储项目跳过 |

---

## 四、详细计划（分层实施）

### 阶段一：感知与控制层（2个月）

#### 第 1 月：中断与传感器驱动

- IMU (ICM20602) 驱动：I2C通信 + 数据就绪中断 + 字符设备读取。
- 超声波 (HC-SR04) 驱动：GPIO触发 + 中断沿计时 + 定时器消抖。
- 要补的实验：14_中断、22_i2c。
- **避坑**：中断里只做时间戳记录和底半部调度，绝不放耗时操作。

#### 第 2 月：执行器与 ROS2 桥接

- PWM 舵机驱动：ioctl 控制角度。
- ROS2 集成：编写 imu_node 和 ultrasonic_node，发布标准 sensor_msgs。
- **避坑**：必须用 ROS2，理解 DDS 的 QoS 实时配置，这是区别于传统嵌入式玩家的标志。

### 阶段二：数据高速公路层（4个月） ⭐ AI芯片岗核心

#### 第 3~4 月：内核内存管理与 mmap

虚拟 NPU 内存管理驱动：
- `alloc_ioctl()`：内核态申请物理连续内存（模拟 NPU 显存）。
- `mmap()`：实现 VMA 映射，让 ROS2/推理引擎直接读写这块内存。
- 要补的知识：页表映射、remap_pfn_range、缓存一致性。

#### 第 5~6 月：DMA 搬运与实时内核

- DMA 驱动：将 IMU/图像数据通过 DMA 搬运到刚才申请的内存池。
- PREEMPT_RT：打补丁，用 cyclictest 测延迟，保证搬运与控制链路的实时性。
- **产出**：一套完整的 Sensor → DMA → mmap → User Space 零拷贝数据通路。

### 阶段三：算力释放与闭环层（6个月） ⭐ 软硬协同核心

#### 第 7~9 月：异步推理框架构建

- NPU 驱动模拟：写一个字符设备模拟真实 NPU，支持 ioctl (下发推理任务) 和 poll (等待计算完成)。
- 自定义算子：在 ONNX Runtime 中注册自定义算子，底层调用你写的 NPU 驱动。
- 中断回调闭环：NPU 计算完毕产生中断 → 驱动唤醒阻塞进程 → 执行器动作。

#### 第 10~12 月：性能优化与论文

- 流水线并行：NPU推理的同时，DMA搬运下一帧数据，掩盖延迟。
- 性能量化：零拷贝前后带宽对比、中断响应延迟、系统吞吐量。
- **产出**：硕士论文《基于零拷贝与异步中断的端侧异构实时推理架构优化》。

---

## 五、xv6 调整（极度精简）

只做与底层系统强相关的，其余全部砍掉：

- **Lab4 (Trap)**：阶段一做传感器中断前必补，理解上下文保存。
- **Lab8 (Lock)**：阶段二做 DMA/mmap 时补，理解多核并发与自旋锁。
- **其余 Lab 全部跳过，绝不恋战。**

---

## 六、双路线面试"变色龙"策略

同样的项目，面对不同公司讲不同故事：

| 公司类型 | 讲什么 | 关键词 |
|----------|--------|--------|
| 大疆/晓辉/宇树（机器人） | 传感器实时采集 + 中断响应 + ROS2 QoS | 实时性、低延迟、DDS |
| 华为/寒武纪/地平线（AI芯片） | DMA零拷贝 + mmap + 异步推理流水线 | 带宽、零拷贝、异构计算 |
| 百度/小马/文远（自动驾驶） | 全链路：传感器→DMA→NPU→控制 | 端到端延迟、功能安全 |

---

## 七、每周节奏

```
周一~周五（每天 2~3 小时）：
  60% 写项目代码 + 调试（遇到内核 Panic 是好事）
  20% 翻阅内核源码或驱动实验补缺
  10% 翻 xv6 理解底层原理
  10% LeetCode (C++，重点刷树/图/动态规划，自动驾驶笔试必考)

周末：
  写技术博客（重点记录：踩坑、架构图、性能对比数据）
  整理本周代码，提交 GitHub (commit message 写英文)
```

---

## 八、里程碑

| 节点 | 预计时间 | 含义 |
|------|---------|------|
| 🏁 M1 | 2026年8月 | 阶段一完成：传感器+PWM+ROS2 跑通，GitHub 仓库公开 |
| 🏁 M2 | 2026年12月 | 阶段二完成：DMA+mmap 零拷贝通路跑通，延迟测试报告输出 |
| 🏁 M3 | 2027年6月 | 阶段三完成：NPU异步推理闭环，硕士论文初稿完成 |

---

## 九、行动清单（未来 48 小时）

1. **淘宝下单**：HC-SR04 + MPU6050/ICM20602 模块（注意电压 3.3V 兼容）。
2. **建 GitHub 仓库**：embodied-ai-linux-platform，用英文写 README 画出三层架构图。
3. **安装 ROS2**：在 Ubuntu 上搭好 ROS2 Humble 环境，跑通官方 Demo。
4. **看芯片手册**：只看 MPU6050 的 WHO_AM_I 和中断寄存器那一页。

---

> 📌 记住：你的核心竞争力不再是"点过了多少个灯"，而是"掌握了数据在硅片与物理世界之间穿梭的规则"。
