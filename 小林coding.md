# 小林 coding — 图解系统笔记

> 来源：小林 coding《图解系统》系列 | https://xiaolincoding.com/

---

# 硬件结构篇

---

## 一、图灵机 —— 计算机的理论老祖

图灵机由三部分组成：

```
┌─────────────────────────────────────────────┐
│  纸带: [ ][ ][1][2][+][ ][ ] ← 连续的格子    │
│         ↑                                  │
│  读写头: 可读写任意格子（= 现代内存）           │
│  内部:  存储单元(存数据) + 控制单元(识别指令)   │
│         + 运算单元(执行运算)                   │
└─────────────────────────────────────────────┘
```

**图灵机执行 1+2 的过程**：

1. 读写头把 `1`、`2`、`+` 写入纸带三个格子，停在 `1` 上
2. 读入 `1` 到存储设备（状态），右移一格
3. 读入 `2` 到状态，右移一格
4. 碰到 `+`，控制单元识别为运算符指令，通知运算单元取出状态中的 `1` 和 `2` 进行相加，结果 `3` 存入状态
5. 运算单元将结果 `3` 返回读写头，写入纸带

---

## 二、冯诺依曼模型 —— 现代计算机的蓝图

1945 年冯诺依曼提出，计算机基本结构分为 **5 个部分**：

```
                      总线（地址/数据/控制）
              ┌──────────────────────────────────┐
  ┌──────┐    │   ┌──────┐   ┌──────┐            │
  │ 输入  │────┼──→│ 运算器│←─→│ 存储器│            │
  │(键盘) │    │   │(ALU) │   │(内存) │            │
  └──────┘    │   └──┬───┘   └──────┘            │
              │      │                            │
  ┌──────┐    │   ┌──┴───┐                        │
  │ 输出  │←───┼───│ 控制器│                        │
  │(屏幕) │    │   └──────┘                        │
  └──────┘    │                                    │
              └──────────────────────────────────┘
```

**CPU 内部组件**：

| 组件 | 名称 | 作用 |
|------|------|------|
| **PC** | 程序计数器 | 存储下一条指令的**内存地址** |
| **IR** | 指令寄存器 | 存放当前正在执行的指令 |
| **通用寄存器** | R0,R1,R2... | 存放需要进行运算的数据 |
| **控制单元** | 控制器 | 负责指挥各部件协调工作 |
| **ALU** | 逻辑运算单元 | 负责算术和逻辑计算 |

**三种总线**：

- **地址总线**：指定内存地址
- **数据总线**：传输数据
- **控制总线**：发送控制信号（读/写等）

---

## 三、a = 1 + 2 的 CPU 执行全流程

### 3.1 编译阶段

C 语言 `a = 1 + 2` → 编译器 → 汇编代码 → 汇编器 → 机器码 → 链接 → 可执行文件。

### 3.2 装入内存

```
内存布局:
  数据段:
    0x100 → 存 1
    0x104 → 存 2
    0x108 → 存 a（结果）

  正文段（指令段）:
    0x200 → load  R0, [0x100]    // 将地址 0x100 的数据 1 装入寄存器 R0
    0x204 → load  R1, [0x104]    // 将地址 0x104 的数据 2 装入寄存器 R1
    0x208 → add   R2, R0, R1     // R2 = R0 + R1 = 3
    0x20c → store [0x108], R2    // 将 R2 的值 3 存入变量 a 的地址 0x108
```

`add` 指令翻译成机器码为 `0x00011020`（32 位二进制）。

### 3.3 CPU 逐条执行 — 四级流水线（指令周期）

每条指令经历 4 个阶段：

| 阶段 | 名称 | 动作 |
|------|------|------|
| ① | **取指 Fetch** | PC 寄存器指向 0x200 → 地址总线传到内存 → 指令 `load` 装入 IR |
| ② | **译码 Decode** | CPU 识别指令类型（load），确定操作数地址 0x100 |
| ③ | **执行 Execute** | 控制单元通过数据总线从 0x100 取到数据 `1`，存入寄存器 R0 |
| ④ | **回写 Store** | PC 自增指向下一条指令 0x204 |

然后重复以上步骤执行后续三条指令，最终 `a=3` 写入地址 0x108。

### 3.4 指令执行速度公式

```
CPU 执行时间 = 指令数 × CPI × 时钟周期时间

其中:
  CPI (Cycles Per Instruction)：每条指令平均所需时钟周期数
  时钟周期 = 1 / CPU 主频  （如 2GHz → 0.5ns）
```

### 3.5 五大指令类型

| 类型 | 例子 | 说明 |
|------|------|------|
| 数据传输类 | load, store, mov | 在寄存器和内存之间搬数据 |
| 运算类 | add, sub, mul, and, or | 算术/逻辑运算 |
| 跳转类 | jmp, branch, call, ret | 改变 PC 的值（if-else/函数调用） |
| 信号类 | trap, int | 触发中断/异常 |
| 闲置类 | nop | 什么都不做 |

---

## 四、存储器金字塔

```
       ┌─────────────┐
       │   寄存器      │  ~0.5ns   │ CPU 内部，容量最小
       │ L1 Cache    │  ~1ns     │ 每核心独有，32KB，SRAM
       │ L2 Cache    │  ~10ns    │ 每核心独有，256KB，SRAM
       │ L3 Cache    │  ~30ns    │ 多核共享，8MB，SRAM
       │   内存 DRAM   │  ~100ns   │ 8-32GB，需定时刷新
       │   SSD        │  ~150μs   │ 固态硬盘
       │   HDD        │  ~10ms    │ 机械硬盘
       └─────────────┘
   速度从快到慢，容量从小到大，价格从贵到便宜
```

> 💡 L1 Cache 比内存快约 **100 倍**，比机械硬盘快 **1000 万倍**。

### 4.1 Cache Line（缓存行）

CPU 不是逐字节读内存，而是每次读一个 **Cache Line（通常 64 字节）**。

Cache 数据结构：

```
内存地址 = Tag(组标记) + Index(索引) + Offset(偏移量)

Cache Line = Valid bit(有效位) + Tag + Data Block(数据块, 64B)
```

**读取过程**：
1. 从内存地址中提取 Index，定位 Cache 中的组
2. 比较 Tag 是否匹配
3. 检查 Valid bit 是否为 1
4. 命中则通过 Offset 从 Data Block 中取出指定字节

### 4.2 提升缓存命中率

**空间局部性**：遍历二维数组时，外层循环行、内层循环列，按内存连续方向访问。

**指令缓存**：有规律的条件分支（if-else）让 CPU 分支预测器更容易命中。

**多核绑定**：把线程绑定到固定 CPU 核心（`sched_setaffinity`），保持 L1/L2 Cache 热度。

---

## 五、写入策略 & 缓存一致性（MESI 协议）

### 5.1 两种写入策略

| 策略 | 做法 | 优缺点 |
|------|------|--------|
| **写直达** Write-Through | 数据同时写入 Cache 和内存 | 简单，数据一致性好，但每次写都要访问内存，慢 |
| **写回** Write-Back | 只写入 Cache，标记为「脏」(Dirty)，Cache Line 被替换时再写回内存 | 快，但多核间数据可能不一致 |

### 5.2 缓存一致性的两个条件

| 条件 | 说明 |
|------|------|
| **写传播** Write Propagation | 某个核心写入时，必须广播通知其他核心 |
| **事务串行化** Transaction Serialization | 各核心看到的数据变化顺序必须一致 |

### 5.3 总线嗅探 + MESI 协议

**总线嗅探**：每个 CPU 核心都监听总线上的广播事件，当其他核心发出写操作时，自己把对应的 Cache Line 标记为失效。

**MESI 四种状态**：

```
     ┌──────────────────────────────────────────────────────┐
     │  M (Modified)    已修改 — 数据是「脏」的，和内存不一致   │
     │  E (Exclusive)   独占   — 干净的，仅一个核心拥有该数据   │
     │  S (Shared)      共享   — 多个核心都有相同数据          │
     │  I (Invalidated) 已失效 — 不可读取，必须从内存重新加载   │
     └──────────────────────────────────────────────────────┘
```

**典型状态流转**：

```
1. A核心读取变量i  → A 的 Cache Line 标记为 E (独占)
2. B核心也读变量i  → A 变 S (共享)，B 变 S (共享)
3. A核心修改变量i  → A 变 M (已修改)，B 变 I (已失效) ← 总线嗅探广播
4. B核心想读变量i  → 先把 A 的数据写回内存，B 重新加载 → 都变 S (共享)
5. A 再次修改 i    → 若还是 M 状态，直接修改无需广播
```

---

## 六、伪共享（False Sharing）

### 6.1 定义

**多个线程同时读写同一个 Cache Line 的不同变量时，导致 CPU Cache 频繁失效的现象。**

### 6.2 产生过程

```
同一个 Cache Line (64字节):
  [    变量A    ][    变量B    ]
       ↑               ↑
    核心1不断写      核心2不断写

流程:
1. 核心1读A → Cache Line 标记 E
2. 核心2读B → Cache Line 变 S (两人共享同一行)
3. 核心1写A → 核心2 的 Cache Line 变 I (失效)，核心1 变 M
4. 核心2写B → 需要先把核心1的数据写回内存 → 再从内存加载 → 核心1 变 I
5. 如此循环往复，Cache 完全失去效果！
```

> 两个线程修改的是**完全不同的变量**（A 和 B），但因为它们在**同一个 Cache Line** 里，导致互相踢对方 Cache，性能暴跌。

### 6.3 解决方案（空间换时间）

**Linux 内核方案**：
```c
#define __cacheline_aligned_in_smp  __attribute__((aligned(64)))
// 确保变量独占一个 Cache Line
```

**Java Disruptor 框架方案**：用继承 + 前后各 7 个 long（56 字节）填充，让热点字段独占一个 Cache Line。

---

## 七、32 位 vs 64 位 CPU

| | 32 位 | 64 位 |
|------|------|------|
| **寻址范围** | 最大 4GB（2³²） | 理论上可达 2⁶⁴（远大于实际物理内存） |
| **一次计算能力** | 最多处理 32bit 数字 | 可处理超过 32bit 的数字 |
| **寄存器位宽** | 32bit | 64bit |
| **兼容性** | — | 64位CPU可以运行32位程序 |
| **操作系统** | 32位OS只能在32位CPU上跑 | 64位OS需要64位CPU |

> 注意：long 类型在 32 位系统是 4 字节，在 64 位 Linux 是 8 字节。跨平台时容易出现「指针强转为 int 就丢失高 32 位」的问题。

---

## 八、CPU 任务调度

### 8.1 调度层级

| 优先级范围 | 调度类 | 调度器 | 策略 |
|------|------|------|------|
| 0~99 | Deadline | Deadline 调度器 | SCHED_DEADLINE |
| 0~99 | Realtime | RT 调度器 | SCHED_FIFO / SCHED_RR |
| **100~139** | **Fair** | **CFS 调度器** | SCHED_NORMAL / SCHED_BATCH |

### 8.2 CFS（完全公平调度）

**核心思想**：为每个任务维护一个 `vruntime`（虚拟运行时间），每次选择 `vruntime` 最小的任务执行。

- nice 值越低（优先级越高）→ 权重越大 → `vruntime` 增长越慢 → 获得更多 CPU 时间
- 使用**红黑树**维护任务队列，查找最小 `vruntime` 是 O(log n)
- `vruntime = 实际运行时间 × (1024 / 进程权重)`

---

## 九、总结：三层打通

```
┌──────────────────────────────────────────────┐
│  应用层 ── 你调用 fork() / write() / mmap()    │  ← 日常记录.md
│            你在「用」CPU                        │
├──────────────────────────────────────────────┤
│  内核层 ── syscall 分发 / 页表翻译 / 调度切换   │  ← xv6.md
│            你在「指挥」CPU                       │
├──────────────────────────────────────────────┤
│  硬件层 ── ALU 算数 / PC 取指 / MMU 查表       │  ← 本篇
│            Cache 缓存 / MESI 一致性             │
│            CPU 到底怎么「干活」                  │
└──────────────────────────────────────────────┘
```

**一条 fork() 的完整链路**：

```
fork() 调用（用户态）
  → 汇编 ecall 指令（用户态 → 内核态）
    → sys_fork() 创建进程结构体、复制页表（内核）
      → 调度时切换 satp 指向新进程页表（内核）
        → MMU 查 TLB → 缺页 → 走三级页表翻译（硬件）
          → 拿到物理地址 → 地址总线找到 DRAM 数据（硬件）
            → 这一切以 Cache Line(64B) 为单位在各级缓存中流动（硬件）
              → 多核时 MESI 协议保证缓存一致性（硬件）
```
